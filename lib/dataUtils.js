export function parseDate(dateStr) { if (!dateStr) return null; const parts = dateStr.split('-'); if (parts.length !== 3) return null; const [day, month, year] = parts; const d = new Date(parseInt(year), parseInt(month) - 1, parseInt(day)); return isNaN(d.getTime()) ? null : d; } function parseCSVLine(line) { const result = []; let current = ''; let inQuotes = false; for (let i = 0; i < line.length; i++) { const char = line[i]; if (char === '"') { if (inQuotes && line[i+1] === '"') { current += '"'; i++; } else inQuotes = !inQuotes; } else if (char === ',' && !inQuotes) { result.push(current); current = ''; } else current += char; } result.push(current); return result; } export function parseCSV(text) { const lines = text.split('\n').filter(l => l.trim()); if (lines.length === 0) return []; const headers = parseCSVLine(lines[0]).map(h => h.trim()); const rows = []; for (let i = 1; i < lines.length; i++) { const values = parseCSVLine(lines[i]); if (values.length < headers.length / 2) continue; const row = {}; headers.forEach((h, idx) => { row[h] = (values[idx] || '').trim(); }); rows.push(row); } return rows; } export function toNum(val) { if (!val || val === 'NA' || val === '') return 0; const n = parseFloat(val.replace(/,/g, '')); return isNaN(n) ? 0 : n; } export function calcPctChange(current, prev) { if (!prev || prev === 0) return null; return ((current - prev) / Math.abs(prev)) * 100; } export function aggregateRows(rows) { let spend = 0, gmv = 0, clicks = 0, conversions = 0; for (const r of rows) { spend += toNum(r.TOTAL_BUDGET_BURNT); gmv += toNum(r.TOTAL_DIRECT_GMV_7_DAYS); clicks += toNum(r.TOTAL_CLICKS); conversions += toNum(r.TOTAL_CONVERSIONS); } const roas = spend > 0 ? gmv / spend : 0; const cpc = clicks > 0 ? spend / clicks : 0; const cvr = clicks > 0 ? (conversions / clicks) * 100 : 0; return { spend, gmv, clicks, conversions, roas, cpc, cvr }; } export function parseZeptoDate(dateStr) { if (!dateStr) return null; const parts = dateStr.split('/'); if (parts.length !== 3) return null; const [day, month, year] = parts; const d = new Date(parseInt(year), parseInt(month) - 1, parseInt(day)); return isNaN(d.getTime()) ? null : d; } export function aggregateZeptoRows(rows) { let spend = 0, directSale = 0, clicks = 0, orders = 0; for (const r of rows) { spend += toNum(r['Spend']); // 'Direct Sales' in Feb/Mar sheets, 'Direct Sale' (no s) in Apr+ sheets directSale += toNum(r['Direct Sales'] || r['Direct Sale'] || 0); clicks += toNum(r['Clicks']); orders += toNum(r['Same_skus']); } const roas = spend > 0 ? directSale / spend : 0; const cpc = clicks > 0 ? spend / clicks : 0; const cvr = clicks > 0 ? (orders / clicks) * 100 : 0; return { spend, gmv: directSale, clicks, conversions: orders, roas, cpc, cvr }; }
